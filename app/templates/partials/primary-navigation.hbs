<nav class="primary-nav-container">
    <ul class="primary-nav-list">
        {{#each global.pages }}
            {{#isnt @key 'index' }}
                <li>
                    <a href="{{ @key }}.html" class="primary-nav-list-link">{{ name }}</a>
                    {{#if sections }}
                        <ul class="nav-dropdown-list">
                            {{#each sections }}
                                <li>
                                    <a href="{{ dasherize ../name }}.html#{{ @key }}" class="nav-dropdown-list-link">{{ name }}</a>
                                </li>
                            {{/each }}
                        </ul>
                    {{/if }}
                </li>
            {{/isnt }}
        {{/each }}
    </ul>
</nav>
